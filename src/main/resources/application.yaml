server:
  port: 8080

spring:
  application:
    name: atm
  datasource:
    url: jdbc:postgresql://localhost:5432/atm
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
